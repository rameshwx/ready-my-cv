name: server_analysis_runner
description: Private stdin/stdout adapter for the canonical Ready My CV workflow.
publish_to: none
version: 1.0.0
resolution: workspace
environment:
  sdk: ^3.8.1
dependencies:
  agent_engine:
    path: ../agent_engine
  catalog_models:
    path: ../catalog_models
  core_models:
    path: ../core_models
  pdf_parser_contract:
    path: ../pdf_parser_contract
